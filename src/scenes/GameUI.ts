import Phaser from 'phaser'

export default class GameUI extends Phaser.Scene
{
	constructor() {
		super('game-ui')
	}

	preload = () => {
        
    }

    create = () => {
        //this.add.text(0,0,"UI working")
    }
}
